describe("Test simple math operations", () => {
  it("should add 1 and 2 to equal 3", () => {
    expect(1 + 2).toEqual(3);
  });

  it("should multiply 2 and 3 to equal 6", () => {
    expect(2 * 3).toEqual(6);
  });
});
